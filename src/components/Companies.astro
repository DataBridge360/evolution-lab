---
const logos = [
  { src: "/assets/images/hero/companies/ypf.webp", alt: "YPF", invert: false, scale: 1 },
  { src: "/assets/images/hero/companies/chevron.webp", alt: "Chevron", invert: false, scale: 1.1 },
  { src: "/assets/images/hero/companies/pampa.webp", alt: "Pampa Energía", invert: false, scale: 1.35 },
  { src: "/assets/images/hero/companies/pluspetrol.svg", alt: "Pluspetrol", invert: false, scale: 1.35 },
  { src: "/assets/images/hero/companies/halliburton.webp", alt: "Halliburton", invert: false, scale: 1.45 },
  { src: "/assets/images/hero/companies/slb.webp", alt: "SLB", invert: false, scale: 0.95 },
  { src: "/assets/images/hero/companies/weatherford.webp", alt: "Weatherford", invert: false, scale: 1 },
  { src: "/assets/images/hero/companies/aesa-blanco.svg", alt: "AESA", invert: true, scale: 1 },
];
---

<section class="companies" id="clientes">
  <div class="companies-inner">
    <h3 class="companies-title">Nuestros Clientes</h3>

    <div class="companies-carousel">
      <div class="companies-track">
        {[...logos, ...logos, ...logos].map((logo) => (
          <div class="companies-logo">
            <img
              src={logo.src}
              alt={logo.alt}
              loading="lazy"
              class={logo.invert ? 'invert-logo' : ''}
              style={`transform: scale(${logo.scale});`}
            />
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
/* ─── COMPANIES ──────────────────────────────────────────── */
.companies {
  background: #fff;
  padding: 40px 0 60px;
  position: relative;
  z-index: 2;
  overflow: hidden;
}

.companies-inner {
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 40px;
}

.companies-title {
  text-align: center;
  font-size: 0.9rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--gray-400);
  margin-bottom: 48px;
}

/* ─── CAROUSEL ───────────────────────────────────────────── */
.companies-carousel {
  position: relative;
  overflow: hidden;
  mask-image: linear-gradient(
    to right,
    transparent 0%,
    black 10%,
    black 90%,
    transparent 100%
  );
  -webkit-mask-image: linear-gradient(
    to right,
    transparent 0%,
    black 10%,
    black 90%,
    transparent 100%
  );
}

.companies-track {
  display: flex;
  gap: 64px;
  align-items: center;
  animation: scroll-infinite 30s linear infinite;
  width: fit-content;
}

.companies-logo {
  flex-shrink: 0;
  width: 160px;
  height: 80px;
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 1;
  transition: transform 0.3s ease;
}

.companies-logo:hover {
  transform: scale(1.06);
}

.companies-logo img {
  max-width: 100%;
  max-height: 100%;
  width: auto;
  height: auto;
  object-fit: contain;
  filter: none;
}

.companies-logo img.invert-logo {
  filter: invert(1);
}

@keyframes scroll-infinite {
  0% {
    transform: translateX(0);
  }
  100% {
    transform: translateX(calc(-160px * 8 - 64px * 8));
  }
}

/* ─── RESPONSIVE ─────────────────────────────────────────── */
@media (max-width: 768px) {
  .companies {
    padding: 60px 0;
  }

  .companies-inner {
    padding: 0 24px;
  }

  .companies-title {
    font-size: 0.8rem;
    margin-bottom: 36px;
  }

  .companies-track {
    gap: 52px;
  }

  .companies-logo {
    width: 120px;
    height: 60px;
  }

  @keyframes scroll-infinite {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(calc(-120px * 8 - 52px * 8));
    }
  }
}
</style>
